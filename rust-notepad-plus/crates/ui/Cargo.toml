[package]
name = "notepad-ui"
version = "8.0.0"
edition = "2021"

[dependencies]
# Local crates
notepad-core = { path = "../core" }
notepad-editor = { path = "../editor" }

# Windows API
windows = { workspace = true }

# Utilities
anyhow = "1.0"
thiserror = "1.0"
log = "0.4"
