[package]
name = "notepad-config"
version = "8.0.0"
edition = "2021"

[dependencies]
# XML parsing
quick-xml = "0.31"
serde = { version = "1.0", features = ["derive"] }

# Utilities
anyhow = "1.0"
thiserror = "1.0"
log = "0.4"
