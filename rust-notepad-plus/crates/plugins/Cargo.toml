[package]
name = "notepad-plugins"
version = "8.0.0"
edition = "2021"

[dependencies]
# Dynamic library loading
libloading = "0.8"

# Windows API
windows = { version = "0.58", features = ["Win32_Foundation"] }

# Utilities
anyhow = "1.0"
thiserror = "1.0"
log = "0.4"
